import random


words = ['python','math','football','tennis']

target = random.choice(words)

game_done = False
guesses = ''

while game_done != True:

    guess = input('Enter a letter to guess: ')

    if guess in target:
        guesses = guesses + 
        
    
